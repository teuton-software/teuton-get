#!/usr/bin/env ruby

$LOAD_PATH.unshift File.expand_path("../../lib", __FILE__)
require "teuton-get/cli"

args = ARGV.clone
if ARGV.include? "--no-color"
  args.delete("--no-color")
  TeutonGet::Format.disable
end

CLI.start(args)
